# Backend для Hack The Ice 7.0

Backend-сервис для платформы социальных льгот и коммерческих предложений, разработанный на Go с использованием Clean Architecture.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Архитектура проекта](#архитектура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Конфигурация](#конфигурация)
- [Миграции](#миграции)
- [API Документация](#api-документация)
- [Разработка](#разработка)
- [Структура проекта](#структура-проекта)

## 🚀 Технологический стек

- **Go** 1.23.0+ - основной язык разработки
- **Gin** - HTTP framework
- **MySQL 8.0** - реляционная база данных
- **Redis** - кэширование и очереди задач
- **Asynq** - фоновая обработка задач
- **SQLX** - работа с БД
- **JWT** - аутентификация
- **Swagger** - API документация
- **Goose** - миграции БД
- **Zap** - логирование
- **Docker & Docker Compose** - контейнеризация

## 🏗 Архитектура проекта

Проект реализован с использованием принципов Clean Architecture:

```
├── cmd/app/              # Точка входа приложения
├── internal/             # Внутренний код приложения
│   ├── api/              # HTTP handlers и middleware
│   ├── domain/           # Бизнес-сущности и интерфейсы
│   ├── service/          # Бизнес-логика
│   ├── repository/       # Слой работы с БД
│   ├── esia/             # Интеграция с ЕСИА
│   ├── queue/            # Система очередей и воркеров
│   ├── cache/            # Кэширование
│   ├── config/           # Конфигурация
│   ├── db/               # Подключение к БД
│   └── server/           # HTTP сервер
├── pkg/                  # Переиспользуемые пакеты
│   ├── auth/             # JWT менеджер
│   ├── email/            # Email сервис
│   ├── hash/             # Хэширование паролей
│   ├── limiter/          # Rate limiting
│   ├── logger/           # Логирование
│   ├── otp/              # OTP коды
│   └── validator/        # Валидация данных
├── migrations/           # SQL миграции
└── docs/                 # Swagger документация
```

## 📦 Установка и запуск

### Предварительные требования

- Go 1.23.0 или выше
- Docker и Docker Compose
- Make

### Быстрый старт

1. **Клонировать репозиторий**
```bash
git clone <repository-url>
cd backend
```

2. **Создать файл `.env`**
```bash
cp .env.example .env
```
Заполните необходимые переменные окружения (см. раздел [Конфигурация](#конфигурация))

3. **Запустить инфраструктуру (MySQL и Redis)**
```bash
make compose
```

4. **Установить goose для управления миграциями**
```bash
make install-goose
```

5. **Применить миграции БД**
```bash
make migration-up
```

6. **Запустить приложение**
```bash
make run
```

Сервер будет доступен по адресу: `http://localhost:8080`

### Остановка инфраструктуры

```bash
make compose-down
```

## ⚙️ Конфигурация

Приложение конфигурируется через переменные окружения. Пример `.env`:

```env
# Общие настройки
ENV=local
LOG_LEVEL=debug
FRONTEND_URL=http://localhost:3000

# HTTP сервер
HTTP_PORT=8080
HTTP_TIMEOUT=4s
HTTP_IDLE_TIMEOUT=60s
HTTP_SWAGGER_ENABLED=true

# База данных MySQL
DB_NET=tcp
DB_SERVER=localhost:3306
DB_NAME=hacktheice
DB_USER=root
DB_PASSWORD=your_password
DB_TIMEZONE=Europe/Moscow
DB_MAX_IDLE_CONNECTIONS=40
DB_MAX_OPEN_CONNECTIONS=40

# Redis
REDIS_TYPE=redis
REDIS_ADDR=localhost:6379
REDIS_POOL_SIZE=70

# JWT аутентификация
JWT_ACCESS_TOKEN_TTL=15m
JWT_REFRESH_TOKEN_TTL=240h
JWT_SIGNING_KEY=your_secret_key

# Безопасность
AUTH_PASSWORD_SALT=your_salt
AUTH_VERIFICATION_CODE_LENGTH=6

# Rate Limiting
LIMITER_RPS=10
LIMITER_BURST=20
LIMITER_TTL=10m

# SMTP для отправки email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_FROM=noreply@example.com
SMTP_PASS=your_password

# Email настройки
EMAIL_ENABLED=true
EMAIL_TEMPLATE_VERIFICATION=verification_template.html

# ЕСИА интеграция
ESIA_BASE_URL=https://esia.gosuslugi.ru
ESIA_CLIENT_ID=your_client_id
ESIA_REDIRECT_URI=http://localhost:8080/api/v1/users/auth/callback
ESIA_SCOPE=openid profile email

# Сервис проверки социальных групп
SOCIAL_GROUP_CHECKER_BASE_URL=https://social-group-checker-mock-production.up.railway.app
```

## 🗄 Миграции

### Просмотр статуса миграций
```bash
make migration-status
```

### Применение миграций
```bash
make migration-up
```

### Откат последней миграции
```bash
make migration-down
```

### Создание новой миграции
```bash
make migration-create
# Введите имя миграции в интерактивном режиме
```

Миграции хранятся в папке `migrations/` и выполняются с помощью инструмента Goose.

## 📚 API Документация

API документация генерируется автоматически с помощью Swagger.

### Генерация документации
```bash
make swag
```

### Просмотр документации

При запущенном приложении с `HTTP_SWAGGER_ENABLED=true`:
- Swagger UI: `http://localhost:8080/swagger/internal/index.html`
- JSON спецификация: `docs/internal_swagger.json`
- YAML спецификация: `docs/internal_swagger.yaml`

### Основные эндпоинты

#### Пользователи
- `POST /api/v1/users/register` - Регистрация пользователя
- `POST /api/v1/users/sign-in` - Вход в систему
- `POST /api/v1/users/refresh` - Обновление токенов
- `GET /api/v1/users/me` - Получение информации о пользователе
- `PUT /api/v1/users/me` - Обновление профиля

#### Льготы
- `GET /api/v1/benefits` - Получение списка льгот
- `GET /api/v1/benefits/:id` - Получение информации о льготе
- `GET /api/v1/benefits/search` - Полнотекстовый поиск льгот
- `GET /api/v1/benefits/categories` - Получение категорий

#### Города
- `GET /api/v1/cities` - Получение списка городов
- `GET /api/v1/cities/:id` - Получение информации о городе

## 🔧 Разработка

### Сборка бинарника
```bash
make build
```

### Запуск линтера
```bash
make lint
```

### Установка зависимостей
```bash
make deps
```

### Фоновые задачи (Workers)

Приложение использует Asynq для асинхронной обработки задач:

- **EmailSender Worker** - отправка email уведомлений
- **SocialGroupChecker Worker** - проверка социальных групп пользователей

Воркеры запускаются автоматически при старте приложения.

### Основные функции

#### Аутентификация
- Регистрация через email/пароль
- Интеграция с ЕСИА (Госуслуги)
- JWT токены (access + refresh)
- Email верификация с OTP кодами

#### Льготы
- Полнотекстовый поиск по льготам
- Фильтрация по категориям, городам, тегам
- Просмотр коммерческих предложений
- Отслеживание просмотров

#### Пользователи
- Управление профилем
- Проверка принадлежности к социальным группам
- Персональные данные из ЕСИА

## 📁 Структура проекта

### Domain Layer (`internal/domain/`)
Содержит бизнес-сущности и ошибки:
- `user.go` - пользователь, токены
- `benefit.go` - льготы и коммерческие предложения
- `city.go` - города и регионы
- `errors.go` - доменные ошибки

### Service Layer (`internal/service/`)
Бизнес-логика приложения:
- `user.go` - управление пользователями
- `benefit_service.go` - работа с льготами
- `city.go` - работа с городами
- `social_group_checker/` - проверка социальных групп

### Repository Layer (`internal/repository/`)
Работа с базой данных:
- `user.go` - пользовательские данные
- `benefit_repository.go` - льготы
- `city.go` - города
- `refresh_session.go` - сессии пользователей

### API Layer (`internal/api/http/`)
HTTP handlers и middleware:
- `internal/v1/` - API версии 1
  - `user.go` - эндпоинты пользователей
  - `benefit.go` - эндпоинты льгот
  - `city.go` - эндпоинты городов
  - `middleware.go` - аутентификация, логирование
  - `errors.go` - обработка ошибок

### Queue System (`internal/queue/`)
Система фоновых задач:
- `client/` - клиент для создания задач
- `processor/` - обработчики задач
- `task/` - определения задач
- `asynqserver/` - конфигурация Asynq

### Packages (`pkg/`)
Переиспользуемые компоненты:
- `auth/` - JWT менеджер
- `email/` - отправка email
- `hash/` - хэширование паролей
- `limiter/` - rate limiting
- `logger/` - структурированное логирование
- `otp/` - генерация OTP кодов
- `validator/` - валидация запросов

## 📄 Лицензия

Проект разработан в рамках хакатона Hack The Ice 7.0.

## 📧 Контакты

По вопросам обращайтесь к команде разработки проекта.
